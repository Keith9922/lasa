# MiniMax OpenAI-compatible API
# 填写以下配置启用 AI 能力。不填则使用本地规则引擎。
MINIMAX_API_KEY=
MINIMAX_BASE_URL=https://api.minimax.io/v1
MINIMAX_MODEL=MiniMax-Text-01

# 语音转录（可选，不填则使用浏览器原生语音识别）
# MiniMax 语音 API 与主 API 共用 MINIMAX_API_KEY
